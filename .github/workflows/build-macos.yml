name: Build macOS DMG

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  build-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 'stable'
          cache: true

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Install dependencies
        run: flutter pub get

      - name: Build Rust library
        run: |
          cd rust_builder
          flutter pub get
          cd ..

      - name: Build macOS app
        run: flutter build macos --release --target-arch=arm64

      - name: Create DMG
        run: |
          # Create DMG using appdmg or native tools
          mkdir -p build/macos/dmg
          cp -r "build/macos/Build/Products/Release/Meridix Launcher.app" build/macos/dmg/
          
          # Create DMG with proper layout
          hdiutil create -volname "Meridix Launcher" \
            -srcfolder build/macos/dmg \
            -ov \
            -format UDZO \
            build/macos/x64/Meridix-Launcher-arm64.dmg

      - name: Sign DMG
        if: ${{ secrets.MACOS_CERTIFICATE != '' }}
        run: |
          codesign -s "${{ secrets.MACOS_CERTIFICATE_ID }}" \
            --options runtime \
            build/macos/x64/Meridix-Launcher-arm64.dmg

      - name: Notarize DMG
        if: ${{ secrets.MACOS_NOTARY_USER != '' }}
        run: |
          xcrun notarytool submit build/macos/x64/Meridix-Launcher-arm64.dmg \
            --apple-id "${{ secrets.MACOS_NOTARY_USER }}" \
            --password "${{ secrets.MACOS_NOTARY_PASSWORD }}" \
            --team-id "${{ secrets.MACOS_TEAM_ID }}" \
            --wait

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: meridix-launcher-macos-arm64
          path: build/macos/x64/Meridix-Launcher-arm64.dmg
          retention-days: 30

  create-release:
    needs: build-macos
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Download macOS artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/**/Meridix-Launcher-arm64.dmg
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
